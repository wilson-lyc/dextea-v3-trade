# {接口名称（中文动宾短语）}

## 接口说明

{用途、调用时机、前置条件、状态流转、幂等与副作用}

## 请求地址

```
{METHOD} /api/v1/{端}/{资源}/{动作}
```

路径参数：

| 参数 | 类型 | 说明 |
| ---- | ---- | ---- |
| {orderId} | long | {订单 ID} |

## 请求头

| 请求头 | 必填 | 说明 |
| ------ | ---- | ---- |
| Authorization | 是 | API 访问令牌，格式 `Bearer {token}` |
| {X-Customer-Id / X-Store-Id} | 是 | {顾客 ID / 门店 ID} |

## 请求参数

### {查询参数 / Body 参数}

| 参数 | 类型 | 必填 | 说明 |
| ---- | ---- | ---- | ---- |
| {field} | {string} | {是} | {业务含义、取值范围、枚举值逐一列出} |

### {items 数组元素}

| 参数 | 类型 | 必填 | 说明 |
| ---- | ---- | ---- | ---- |
| {skuId} | {string} | {是} | {商品 SKU 编码} |

## 请求示例

```bash
curl -X {METHOD} http://localhost:8080/api/v1/{path} \
  -H "Authorization: Bearer {token}" \
  -H "{X-Customer-Id}: 1" \
  -H "Content-Type: application/json" \
  -d '{}'
```

## 响应格式

`data` 字段：

| 参数 | 类型 | 说明 |
| ---- | ---- | ---- |
| {field} | {long} | {说明} |

## 响应示例

```json
{
  "code": 0,
  "message": "成功",
  "data": {}
}
```

## 错误码字典

| 错误码 | HTTP 状态码 | 说明 |
| ------ | ----------- | ---- |
| 50000 | 500 | 系统繁忙，请稍后重试 |
| 50101 | 500 | 数据库访问异常（系统繁忙，请稍后重试） |
| {21016} | 400 | {订单不存在} |
| 40001 | 400 | 缺少请求头 |
| 40002 | 400 | 参数缺失 / 参数校验失败 |
| 40100 | 401 | 未登录 |
