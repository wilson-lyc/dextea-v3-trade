# Product

## Register

product

## Users

「德贤茶庄」交易中台（dextea-trade）的调用方与运维人员。渠道等外部系统调用交易 API 时携带令牌完成鉴权；运维通过环境变量管理合法令牌。

## Product Purpose

dextea-trade 是茶庄线上点餐系统的交易中台（订单创建、支付宝支付、取餐码、订单状态流转）。所有 API 请求须通过令牌鉴权：合法令牌通过环境变量 `AUTH_TOKENS` 配置（逗号分隔，命中其一即可），服务启动时加载进内存完成校验，不落数据库、无管理界面。鉴权可通过 `AUTH_ENABLED` 总开关启停（默认开启）。成功标准：调用方接入简单（配置即用），令牌轮换只需改环境变量重启，不引入额外存储与运维负担。
